{
  "name": "adventofcode22",
  "version": "1.0.0",
  "description": "Contains solutions to Advent of Code challenges",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "solve": "npm run clean && npm run lint && npx ts-node src/index.ts",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "author": "Will Shivers",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "axios": "^1.2.0",
    "minimist": "^1.2.7"
  }
}
